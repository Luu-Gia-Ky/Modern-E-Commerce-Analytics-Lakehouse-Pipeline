name: 'ecommerce_dbt'
version: '1.0.0'
profile: 'ecommerce_dbt'
model-paths: ["models"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
test-paths: ["tests"]

models:
  ecommerce_dbt:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table